import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ExchangeDetails = ({ data }) => {
  return (
    <View>
      <Text>ExchangeDetails</Text>
    </View>
  );
};

export default ExchangeDetails;

const styles = StyleSheet.create({});
