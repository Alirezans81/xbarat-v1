import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DepositForm = () => {
  return (
    <View>
      <Text>DepositForm</Text>
    </View>
  );
};

export default DepositForm;

const styles = StyleSheet.create({});
