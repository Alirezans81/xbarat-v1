import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TransferForm = () => {
  return (
    <View>
      <Text>TransferForm</Text>
    </View>
  );
};

export default TransferForm;

const styles = StyleSheet.create({});
